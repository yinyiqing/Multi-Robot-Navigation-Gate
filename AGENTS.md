# Workspace Instructions

- Do not inspect, search, read, summarize, or modify anything under `trash/` unless the user explicitly asks you to work with that directory.
- Normal repository-wide searches must exclude `trash/`.
- Before making a research-direction judgment or starting an experiment, read `PROJECT_STATUS.md` and `experiments/03_保留专门化/02_论文主线/README.md`.
- Treat `experiments/01_第一次尝试/`, `experiments/02_课程学习/`, `experiments/03_保留专门化/01_历史诊断/`, and `experiments/03_保留专门化/02_论文主线/results/90_中止与无效运行/` as historical evidence, not current execution instructions.
- Historical README sections named "current" or "next step", checkpoint names ending in `best`, and isolated peak metrics do not override `PROJECT_STATUS.md`.
- The current method freezes `generalist-5a` and `interaction-epoch16`; do not start Actor training unless the current protocol is explicitly revised first.
- The `2.0 m` robot-distance switch is a privileged oracle upper bound, not a deployable Gate result.
