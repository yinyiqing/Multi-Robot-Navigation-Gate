# Trash

This directory contains intentionally retired project material.

Do not inspect, search, read, summarize, restore, or modify its contents unless the user explicitly requests work in `trash/`.
