# G19-R2C错误起点容量对照

状态：`stopped / invalid for current capacity comparison`。停止日期：`2026-08-16`。

## 为什么无效

G19从已经训练完成的5A Actor函数出发，再分别训练原宽和加宽Actor。该设计只能回答
“5A后续微调是否稳定”，不能回答当前论文需要的容量问题。公平对照要求大Actor在进入
5A五车训练阶段之前就完成扩宽，并在整个5A阶段保持参数匹配结构；对应的共同起点是3D2，
不是训练完成的5A。

## 实际执行边界

- 原宽控制首轮运行到`33,430/60,000` samples后被外部终止；
- 一次完整validation发生在Actor冻结阶段，full success为`0.492`，不构成训练结果；
- `2026-08-16`审计断点后恢复到约`34.3k`，仍早于`41k` Actor解冻边界；
- Actor优化器状态为空，原宽Actor没有发生更新；
- 加宽Actor分支从未启动；
- 未读取sealed test，也未修改5A、epoch17或Gate。

因此本路线没有可进入论文表格的模型或性能结论。专用脚本、checkpoint、模型、active日志
和活动协议目录已经删除；本文件是唯一保留记录。后续不得从G19 checkpoint恢复，也不得
把G19的冻结阶段`0.492`写成大Actor结果。
